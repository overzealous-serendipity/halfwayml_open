// Your Next.js configuration
const nextConfig = {
  reactStrictMode: true,
  output: "standalone",
  env: {},
};

// Wrap your Next.js configuration with the bundle analyzer configuration
module.exports = nextConfig;
